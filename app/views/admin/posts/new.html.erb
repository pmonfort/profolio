<% content_for :title, "Admin - New post" %>

<div class="w-full max-w-4xl mx-auto">
  <h1 class="text-4xl md:text-5xl font-bold text-slate-900 dark:text-white mb-8">Admin - New post</h1>

  <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8 md:p-12">
    <%= render "form", post: @post %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to admin posts", admin_posts_path, class: "inline-flex items-center text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 transition-colors" %>
  </div>
</div>

