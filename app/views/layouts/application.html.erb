<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Portfolio" %> - Pablo Monfort</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Software Developer Portfolio and Blog">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script>
      (function() {
        if (typeof window === "undefined") return;
        const allowedThemes = ["classic", "dark"];
        const legacyThemeMap = { midnight: "dark" };

        try {
          const storedTheme = window.localStorage.getItem("preferred-theme");
          const resolvedTheme = legacyThemeMap[storedTheme] || storedTheme;

          if (resolvedTheme && allowedThemes.includes(resolvedTheme)) {
            document.documentElement.setAttribute("data-theme", resolvedTheme);
            if (resolvedTheme !== storedTheme) {
              window.localStorage.setItem("preferred-theme", resolvedTheme);
            }
          } else if (storedTheme) {
            window.localStorage.removeItem("preferred-theme");
          }
        } catch (error) {
          console.warn("Theme preference unavailable", error);
        }
      })();
    </script>
  </head>

  <body class="antialiased theme-body">
    <div class="min-h-screen site-background">
      <nav class="site-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center space-x-4">
              <div data-controller="theme" class="theme-toggle" data-theme-target="container">
                <button type="button"
                        class="theme-toggle-button"
                        role="switch"
                        aria-checked="false"
                        data-action="theme#toggleClick keydown->theme#handleKeydown"
                        data-theme-target="button">
                  <span class="theme-toggle-track">
                    <span class="theme-toggle-thumb"></span>
                  </span>
                  <span class="theme-toggle-label" data-theme-target="label">Classic</span>
                </button>
              </div>
              <%= link_to root_path, class: "nav-brand transition-colors" do %>
                Pablo Monfort
              <% end %>
            </div>
            <div class="flex items-center space-x-6 md:space-x-8">
              <%= link_to "Home", root_path, class: "nav-link transition-colors font-medium#{' nav-link-active' if request.path == root_path}" %>
              <%= link_to "Blog", blog_path, class: "nav-link transition-colors font-medium#{' nav-link-active' if request.path.start_with?('/blog')}" %>
              <% if user_signed_in? %>
                <%= link_to "Posts", posts_path, class: "nav-link transition-colors font-medium#{' nav-link-active' if request.path.start_with?('/posts') || request.path.start_with?('/admin')}" %>
                <%= link_to "Profile", profile_path, class: "nav-link transition-colors font-medium#{' nav-link-active' if request.path == profile_path}" %>
                <div class="flex items-center space-x-4 pl-4 border-divider">
                  <span class="text-sm nav-muted hidden sm:inline"><%= current_user.email %></span>
                  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link transition-colors font-medium cursor-pointer" %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </nav>

      <main class="theme-content max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8 md:py-12">
        <%= yield %>
      </main>

      <footer class="site-footer mt-16 py-8">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center text-sm nav-muted">
            <p>&copy; <%= Date.current.year %> Pablo Monfort. All rights reserved.</p>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
