<% content_for :title, "New post" %>

<div class="w-full max-w-4xl mx-auto">
  <h1 class="text-4xl md:text-5xl font-bold text-neutral-900 dark:text-neutral-50 mb-8">New post</h1>

  <div class="bg-white dark:bg-neutral-900 rounded-2xl shadow-xl border border-neutral-100 dark:border-neutral-800 p-8 md:p-12">
    <%= render "form", post: @post %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to posts", posts_path, class: "inline-flex items-center text-violet-600 dark:text-violet-400 hover:text-violet-800 dark:hover:text-violet-300 transition-colors font-medium" %>
  </div>
</div>
