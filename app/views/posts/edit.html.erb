<% content_for :title, "Editing post" %>

<div class="w-full max-w-4xl mx-auto">
  <h1 class="text-4xl md:text-5xl font-bold text-slate-900 dark:text-white mb-8">Editing post</h1>

  <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8 md:p-12">
    <%= render "form", post: @post %>
  </div>

  <div class="mt-6 flex flex-wrap gap-3">
    <%= link_to "Show this post", @post, class: "px-4 py-2 bg-slate-100 dark:bg-slate-700 hover:bg-slate-200 dark:hover:bg-slate-600 text-slate-900 dark:text-white rounded-md font-medium transition-colors" %>
    <%= link_to "← Back to posts", posts_path, class: "px-4 py-2 bg-slate-100 dark:bg-slate-700 hover:bg-slate-200 dark:hover:bg-slate-600 text-slate-900 dark:text-white rounded-md font-medium transition-colors" %>
  </div>
</div>
